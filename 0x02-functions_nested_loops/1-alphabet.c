#include "holberton.h"

/**
*function prints the alphabet in lowercase followed by a new line
* return type is void
*/

void print_alphabet(void);
{
char alpha = a;
while (a <= z)

{
_putchar(alpha);
alpha++;
}
_putchar('\n');
}
